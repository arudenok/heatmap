services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - heatmap-data:/app/data
    environment:
      JWT_SECRET: c2VjcmV0LWtleS1mb3ItaGVhdG1hcC1yZWdpc3RyeS1kZXYtb25seS1jaGFuZ2UtbWU=
      CORS_ORIGINS: http://localhost:5173,http://localhost:8081

  frontend:
    build: ./frontend
    ports:
      - "8081:80"
    depends_on:
      - backend

volumes:
  heatmap-data:
